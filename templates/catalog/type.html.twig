{% extends 'base.html.twig' %}

{% block title %}Produit {{type.name}}{% endblock %}

{% block body %}
    <section class="hero">
        <div class="container">
        <!-- Breadcrumbs -->
        <ol class="breadcrumb justify-content-center">
            <li class="breadcrumb-item"><a href="{{path('app_main_home')}}">Home</a></li>
            <li class="breadcrumb-item active">{{type.name}}</li>
        </ol>
      <!-- Hero Content-->
        <div class="hero-content pb-5 text-center">
            <h1 class="hero-heading">{{type.name}}</h1>
            <div class="row">
            <div class="col-xl-8 offset-xl-2">
                <p class="lead text-muted">{{type.subtitle}}</p>
            </div>
            </div>
        </div>
        </div>
  </section>

  <section class="products-grid">
    <div class="container-fluid">

      {# inclure le header pour la pagination #}

    {% include 'partials/_header_pruductList.html.twig'%} 
    {# fin header #}

      <div class="row">
        {% for product in productList %}
          <!-- product-->
        <div class="product col-xl-3 col-lg-4 col-sm-6">
            <div class="product-image">
              <a href="product.html" class="product-hover-overlay-link">
                <img src="{{asset (product.picture) | replace ({'.jpg' : '_tn.jpg'}) }}" alt="product" class="img-fluid">
              </a>
            </div>
            <div class="product-action-buttons">
              <a href="#" class="btn btn-outline-dark btn-product-left"><i class="fa fa-shopping-cart"></i></a>
              <a href="product.html" class="btn btn-dark btn-buy"><i class="fa-search fa"></i><span class="btn-buy-label ml-2">Voir</span></a>
            </div>
            <div class="py-2">
              <p class="text-muted text-sm mb-1">{{product.type.name}}</p>
              <h3 class="h6 text-uppercase mb-1"><a href="product.html" class="text-dark">{{product.name}}</a></h3><span class="text-muted">{{product.price}} €</span>
            </div>
        </div>
          <!-- /product-->
        {% endfor %}

      </div>
      
    </div>
  </section>
{% endblock %}
