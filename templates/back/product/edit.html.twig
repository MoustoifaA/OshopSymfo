{% extends 'base.html.twig' %}

{% block title %}Edit Product{% endblock %}

{% block body %}
    <div class="container">
    <h1>Edit Product</h1>

    {{ include('back/product/_form.html.twig', {'button_label': 'Update'}) }}

    <a title="back to the product list" class="btn btn-primary m-2" href="{{ path('app_back_product_browse') }}">Back to product list</a>

    {{ include('back/product/_delete_form.html.twig') }}
    </div>
{% endblock %}
