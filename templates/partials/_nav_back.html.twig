<nav class="navbar navbar-expand-lg navbar-sticky navbar-airy navbar-light">
      <div class="container-fluid">
        <!-- Navbar Header  -->
        <a href="{{path('app_back_main')}}" class="navbar-brand">oShop Back office</a>
        <button type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse"
          aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler navbar-toggler-right"><i class="fa fa-bars"></i></button>
        <!-- Navbar Collapse -->
        <div id="navbarCollapse" class="collapse navbar-collapse">
          <ul class="navbar-nav mx-auto">
            <li class="nav-item">
              <a href="{{path('app_back_main')}}" class="nav-link {% if app.request.requesturi == "/back/" %} active {% endif %}">Home</a>
            </li>
            <li class="nav-item">
              <div class="dropdown">
                <a class="nav-link dropdown-toggle {% if 'back/category' in app.request.uri %} active {% endif %}" href="#" data-toggle="dropdown">Catégories Manager</a>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                    <a class='dropdown-item' href="{{ path('app_back_category_browse') }}">List</a>
                    <a class='dropdown-item' href="{{ path('app_back_category_add') }}">Add category</a>
                </div>
              </div>
            </li>
            <li class="nav-item">
              <div class="dropdown">
                <a class="nav-link dropdown-toggle {% if 'back/product' in app.request.uri %} active {% endif %}" href="#" data-toggle="dropdown">Products Manager</a>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                   <a class='dropdown-item' href="{{ path('app_back_product_browse') }}">List</a>
                    <a class='dropdown-item' href="{{ path('app_back_product_add') }}">Add product</a>
                </div>
              </div>
            </li>
            <li class="nav-item">
              <div class="dropdown">
                <a class="nav-link dropdown-toggle {% if 'catalog/brand' in app.request.uri %} active {% endif %}" href="#" data-toggle="dropdown">Brands Manager</a>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                    <a class='dropdown-item' href="{{ path('app_back_brand_index') }}">List</a>
                    <a class='dropdown-item' href="{{ path('app_back_brand_new') }}">Add brand</a>
                </div>
              </div>
            </li>
          </ul>
        </div>
      </div>
    </nav>