<nav class="navbar navbar-expand-lg navbar-sticky navbar-airy navbar-light">
      <div class="container-fluid">
        <!-- Navbar Header  -->
        <a href="{{path('app_back_main')}}" class="navbar-brand">oShop Back office</a>
        <button type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse"
          aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler navbar-toggler-right"><i class="fa fa-bars"></i></button>
        <!-- Navbar Collapse -->
        <div id="navbarCollapse" class="collapse navbar-collapse">
          <ul class="navbar-nav mx-auto">
            <li class="nav-item">
              <a href="{{path('app_back_main')}}" class="nav-link {% if app.request.requesturi == "/back/" %} active {% endif %}">Accueil</a>
            </li>
            <li class="nav-item">
              <div class="dropdown">
                <a class="nav-link dropdown-toggle {% if 'catalog/category' in app.request.uri %} active {% endif %}" href="#" data-toggle="dropdown">Catégories</a>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                    <a class='dropdown-item' href="{{ path('app_back_category_add') }}">categories list</a>
                    <a class='dropdown-item' href="{{ path('app_back_category_browse') }}">New category</a>
                </div>
              </div>
            </li>
            <li class="nav-item">
              <div class="dropdown">
                <a class="nav-link dropdown-toggle {% if 'catalog/type' in app.request.uri %} active {% endif %}" href="#" data-toggle="dropdown">Types de produits</a>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                  {% for type in navList.getType %}
                    <a class="dropdown-item" href="{{path("app_catalog_type", {id:type.id} )}}">{{type.name}}</a>
                  {% endfor %}
                </div>
              </div>
            </li>
            <li class="nav-item">
              <div class="dropdown">
                <a class="nav-link dropdown-toggle {% if 'catalog/brand' in app.request.uri %} active {% endif %}" href="#" data-toggle="dropdown">Marques</a>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                  {% for brand in navList.getBrand %}
                    <a class="dropdown-item" href="{{path("app_catalog_brand", {id:brand.id} )}}">{{brand.name}}</a>
                  {% endfor %}
                </div>
              </div>
            </li>
          </ul>
        </div>
      </div>
    </nav>